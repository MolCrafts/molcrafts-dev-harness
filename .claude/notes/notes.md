# Notes — molcrafts-harness

Evolving decisions. `/mol:note` reconciles entries here; a rule that admits no
exception belongs in `law.md` instead.

The marketplace vs product-capability split (skills are procedures; product
capability is an MCP tool; never a skill that copies a tool) is law:
`law.md` `no-skill-copy-of-tool`. Do not restate it here.
